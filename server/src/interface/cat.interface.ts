export interface CreateCatDto {
  readonly name: string
  readonly age: number
  readonly gender: boolean
}

export interface Cat {
  readonly name: string
  readonly age: number
  readonly gender: boolean
}